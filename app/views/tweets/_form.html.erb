<%= form_with(model: tweet, local: true) do |form| %><%#@を消しているrenderの先は@を消したほうが望ましいため %>
	<%= form.text_field :image, placeholder: "Image Url" %>
	<%= form.text_area :text, placeholder: "text", rows: "10" %>
	<%= form.submit "SEND" %>
<% end %>